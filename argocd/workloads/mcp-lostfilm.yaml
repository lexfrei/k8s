apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mcp-lostfilm
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: workloads
  destination:
    namespace: mcp-lostfilm
    server: "https://kubernetes.default.svc"
  source:
    repoURL: https://github.com/lexfrei/k8s.git
    targetRevision: HEAD
    path: manifests/mcp-lostfilm
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
