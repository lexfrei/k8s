apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vpa
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: infra
  destination:
    namespace: vpa-system
    server: "https://kubernetes.default.svc"
  sources:
    - repoURL: "ghcr.io/stevehipwell/helm-charts"
      targetRevision: "1.12.0"
      chart: vertical-pod-autoscaler
      helm:
        valuesObject:
          serviceMonitor:
            enabled: true
          recommender:
            extraArgs:
              # Seed the usage histogram from VictoriaMetrics so recommendations
              # survive recommender restarts and are useful immediately instead
              # of after days of in-memory accumulation.
              - --storage=prometheus
              - --prometheus-address=http://vmsingle-vmsingle.monitoring.svc:8429
              - --prometheus-cadvisor-job-name=monitoring/kubelet-cadvisor
              # 8d/15m returned ~11M datapoints and blew through vmsingle's
              # per-query memory budget (422: not enough memory), leaving the
              # recommender with no history at all. 2d/30m fits comfortably and
              # still covers most of the histogram weight given the 24h decay
              # half-life.
              - --history-length=2d
              - --history-resolution=30m
              - --container-pod-name-label=pod
              - --container-name-label=container
              - --container-namespace-label=namespace
            resources:
              requests:
                cpu: 50m
                memory: 256Mi
              limits:
                memory: 512Mi
          updater:
            resources:
              requests:
                cpu: 25m
                memory: 128Mi
              limits:
                memory: 256Mi
          admissionController:
            resources:
              requests:
                cpu: 25m
                memory: 64Mi
              limits:
                memory: 128Mi
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
