---
# Kubernetes cluster inventory (k3s-ansible format)
# Groups: server (control plane), agent (workers)
# Cross-cutting groups: rpi (Raspberry Pi nodes), storage (ZFS storage nodes)
all:
  children:
    k3s_cluster:
      children:
        # K3s server nodes (control plane)
        server:
          hosts:
            k8s-storage-01:
              ansible_host: 172.16.101.4
              # Host-specific entries merged into server_config_yaml via
              # combine() in group_vars/k3s_cluster.yaml. Cluster-wide
              # entries (e.g. secrets-encryption) live there.
              server_config_extra:
                # ZFS ARC is capped at 8 GiB — reserve from kubelet scheduling
                kubelet-arg:
                  - "system-reserved=memory=8Gi"
                  # Cap PIDs per pod so a single fork-bomb cannot exhaust the
                  # node PID space (CIS 4.2.13). 16384 is ~15x the observed
                  # peak (~1073) yet far below the 4M node pid_max.
                  - "pod-max-pids=16384"
                  # Keep 20 dead instances per container instead of the default
                  # 1, so a crash loop leaves a trail. restartCount is only a
                  # counter — without this, kubelet's GC reaps every previous
                  # container within a minute and its log goes with it, leaving
                  # `kubectl logs --previous` (a boolean, no depth argument) as
                  # the single step back. Unrelated to containerLogMaxFiles,
                  # which rotates the log of the LIVE container.
                  # The flag is marked deprecated upstream but still functional.
                  - "maximum-dead-containers-per-container=20"

        # K3s agent nodes (workers) — none currently
        agent:
          hosts: {}

      vars:
        # K3s version for upgrades
        # Note: k3s-ansible requires explicit version, cannot use "stable" or "latest"
        k3s_version: v1.36.3+k3s1

    # Cross-cutting groups for platform-specific configuration

    # ZFS storage nodes (x86_64, LSI SAS HBA, Intel AHCI, USB boot chain)
    storage:
      hosts:
        k8s-storage-01:
