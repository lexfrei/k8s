apiVersion: v1
kind: Service
metadata:
  name: mcp-rutracker
  namespace: mcp-rutracker
spec:
  selector:
    app.kubernetes.io/name: mcp-rutracker
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
