apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mcp-rutracker-session
  namespace: mcp-rutracker
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: zfs-localpv
  resources:
    requests:
      storage: 128Mi
