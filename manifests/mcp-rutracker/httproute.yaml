apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: mcp-rutracker
  namespace: mcp-rutracker
  annotations:
    internal-dns/hostname: rutracker-mcp.home.lex.la
spec:
  parentRefs:
    - name: cilium-gateway-internal
      namespace: kube-system
      sectionName: https-home-lex-la
  hostnames:
    - rutracker-mcp.home.lex.la
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: mcp-rutracker
          port: 8080
