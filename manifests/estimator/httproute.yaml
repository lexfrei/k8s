apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: estimator
  annotations: {}
spec:
  parentRefs:
    - name: cilium-gateway
      namespace: kube-system
      sectionName: https-eta-lex-la
  hostnames:
    - eta.lex.la
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: estimator
          port: 8080
