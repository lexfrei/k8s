apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: estimator
  annotations: {}
spec:
  parentRefs:
    - name: cloudflare-tunnel
      namespace: cloudflare-tunnel-system
  hostnames:
    - eta.lex.la
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: estimator
          port: 8080
