apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: authelia-postgres
  namespace: security
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: authelia
      cnpg.io/podRole: instance
  podMetricsEndpoints:
    - port: metrics
