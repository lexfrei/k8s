apiVersion: v1
kind: Service
metadata:
  name: mcp-transmission
  namespace: transmission-system
spec:
  selector:
    app.kubernetes.io/name: mcp-transmission
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
