apiVersion: v1
kind: Service
metadata:
  name: claude-workspace
  namespace: claude-workspace
  labels:
    app: claude-workspace
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: claude-workspace
