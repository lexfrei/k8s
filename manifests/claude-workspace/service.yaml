apiVersion: v1
kind: Service
metadata:
  name: claude-workspace
  namespace: claude-workspace
  labels:
    app: claude-workspace
spec:
  type: ClusterIP
  ports:
    - port: 4180
      targetPort: 4180
      protocol: TCP
      name: http
  selector:
    app: claude-workspace
