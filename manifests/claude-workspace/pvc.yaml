apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: claude-workspace-home
  namespace: claude-workspace
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: zfs-localpv
  resources:
    requests:
      storage: 50Gi
