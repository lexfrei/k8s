apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gokapi
  namespace: gokapi
  labels:
    app: gokapi
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: zfs-localpv
  resources:
    requests:
      storage: 500Gi
