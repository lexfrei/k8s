apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mcp-myshows-session
  namespace: mcp-myshows
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: zfs-localpv
  resources:
    requests:
      storage: 128Mi
