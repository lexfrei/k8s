apiVersion: v1
kind: Service
metadata:
  name: mcp-myshows
  namespace: mcp-myshows
spec:
  selector:
    app.kubernetes.io/name: mcp-myshows
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
