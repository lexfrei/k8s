extends: relaxed

ignore:
  - secrets/
  - ansible/
  # Vendored Helm charts contain Go template syntax that is not valid YAML
  - charts/

rules:
  line-length: disable
  truthy: disable
  comments-indentation: disable
